# Ch 25 kernel lab. Runs inside the QEMU VM described in Ch 24 with the
# vuln_mod LKM loaded. Not exercised by the Linux CI runners.
.PHONY: all help clean
help:
	@echo "Ch 25: Linux kernel UAF-to-controlled-object lab."
	@echo "Boot the QEMU VM from ch24-kernel-intro/vm/run.sh and load vuln_mod."
	@cat .ci-skip
all:
	@$(MAKE) help
clean:
	@:
