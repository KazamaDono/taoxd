# common/

Shared helpers used across chapters:

- `exploit_helpers.py` — pwntools `start()`, leak logging, chain packing.
- `harness.h` — I/O setup and teaching scaffolds for vulnerable C targets.

Lab use only.
