# This chapter's runnable exploit needs the pinned patched d8 from Ch 21.
# See .ci-skip and the chapter README.
.PHONY: all help clean
help:
	@echo "Ch 22: R/W -> RCE in a hardened renderer."
	@echo "Requires the pinned d8 build from ch21-jit/build.sh (see chapter for steps)."
	@cat .ci-skip
all:
	@$(MAKE) help
clean:
	@:
